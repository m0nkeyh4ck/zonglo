data class Producto(
    val titulo: String = "Sin título",
    val descripcion: String = "",
    val precio: Double = 0.0,
    val valor_envio: Double = 0.0,
    val imagenes: List<String> = emptyList() // Si el JSON no trae esto, evita el crash
)
